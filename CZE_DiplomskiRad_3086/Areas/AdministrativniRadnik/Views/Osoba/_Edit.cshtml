@*@model CZE.Web.Areas.AdministrativniRadnik.Models.OsobaEditVM
@{
  Layout = null;
}

  <ul class="nav nav-tabs" id="myTabList" role="tablist">
    <li class="nav-item">
      <a class="nav-link btn btn-outline-secondary active" id="osoba-tab" data-toggle="tab" href="#osoba" role="tab" aria-controls="osoba" aria-selected="true">Osoba</a>
    </li>
    @if (Model.ZaposlenikCreateVM.ZaposlenikVM != null)
    {
      <li class="nav-item">
        <a class="nav-link btn btn-outline-secondary" id="zaposlenik-tab" data-toggle="tab" href="#zaposlenik" role="tab" aria-controls="zaposlenik" aria-selected="false">Zaposlenik</a>
      </li>
    }
    @if (Model.KandidatCreateVM.KandidatVM != null)
    {
      <li class="nav-item">
        <a class="nav-link btn btn-outline-secondary" id="kandidat-tab" data-toggle="tab" href="#kandidat" role="tab" aria-controls="kandidat" aria-selected="false">Kandidat</a>
      </li>
    }
  </ul>
  <div class="tab-content" id="myTabContent">
    <div class="tab-pane fade show active " id="osoba" role="tabpanel" aria-labelledby="osoba-tab">
      @{await Html.RenderPartialAsync("_Create",Model.OsobaCreateVM);}
    </div>

    @if (Model.ZaposlenikCreateVM.ZaposlenikVM != null)
    {
      <div class="tab-pane fade " id="zaposlenik" role="tabpanel" aria-labelledby="zaposlenik-tab">
        @{ await Html.RenderPartialAsync("~/Areas/AdministrativniRadnik/Views/Zaposlenik/_Create.cshtml", Model.ZaposlenikCreateVM);}
      </div>
    }
    @if (Model.KandidatCreateVM.KandidatVM != null)
    {
      <div class="tab-pane fade " id="kandidat" role="tabpanel" aria-labelledby="kandidat-tab">
        @{ await Html.RenderPartialAsync("~/Areas/AdministrativniRadnik/Views/Kandidat/_Create.cshtml", Model.KandidatCreateVM);}
      </div>
    }

  </div>*@
